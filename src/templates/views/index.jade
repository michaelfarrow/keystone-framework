extends ../layouts/base

block head
  script.
    var version = '#{keystone.version.match(/^(\d+\.\d+\.\d+)/)[0]}';

block content
  .container
    .content
      .title Keystone
      #root.subtitle
